# Changes

Add one Markdown file for each meaningful change. Use `patch` by default; use
`minor` only when the user explicitly requests a minor release, and `major`
only when the user explicitly requests a major release.

```md
---
type: patch
---

Explain the user-visible change in release-note language.
```
